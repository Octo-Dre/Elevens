package Activity04;


import static org.junit.Assert.*;
import org.junit.After;
import org.junit.Before;
import org.junit.Test;

/**
 * The test class DeckTesterTest.
 *
 * @author  (your name)
 * @version (a version number or a date)
 */
public class DeckTesterTest
{
    /**
     * Default constructor for test class DeckTesterTest
     */
    public DeckTesterTest()
    {
    }

    /**
     * Sets up the test fixture.
     *
     * Called before every test case method.
     */
    @Before
    public void setUp()
    {
    }

    /**
     * Tears down the test fixture.
     *
     * Called after every test case method.
     */
    @After
    public void tearDown()
    {
    }
    /*
    @Test
    public void testArePermutations()
    {
        String[] ranks = {"jack", "queen", "king"};
        String[] suits = {"blue", "red"};
        int[] pointValues = {11, 12, 13};
        Deck d = new Deck(ranks, suits, pointValues);
        Deck e = new Deck(ranks, suits, pointValues);
        assertEquals(true, DeckTester.arePermutations(d, e));
    } */
}

